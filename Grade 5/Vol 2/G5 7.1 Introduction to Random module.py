# # random means something that happens by chance and is not fixed every time.
# import random on line 6 module used to generate random numbers.
# random.randint(1,50) on line 7 generates a random integer between 1 and 50 (inclusive).

# Task : Change the (1, 50) to (1,20) on line 7 and run the code.

import random
secret_number = random.randint(1, 50)
print(secret_number)

